{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // make connection string so we can refer to it in program
  "ConnectionStrings": {
    "TaskConnection": "Data Source = TaskDatabase.sqlite"
  }
}
